import styled from 'styled-components';

const ErrorMessage = styled.div`
  font-size: 16px;
  font-weight: 600;
  color: #fefefe;
  text-align: center;
  margin: 16px 0;
`;

export default ErrorMessage;
